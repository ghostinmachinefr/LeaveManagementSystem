import React from 'react';

const Default = () => {
    return (
        <div>
            <h1>Welcome to the User Panel</h1>
            <p>This is the default page for the user section.</p>
        </div>
    );
};

export default Default;